export interface singleChallanEntry {
  category: string;
  brand: string;
  model: string;
  qty: number;
  createdAt: string;
}

export interface cStockChallans {
  brand: string;
  model: string;
  qty: number;
  category: string;
  createdAt: string;
}
